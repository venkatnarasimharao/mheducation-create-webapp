import { XmlTransformerUtil } from './xml-transformer.util';

describe('XmlTransformerUtil', () => {
  it('should create an instance', () => {
    expect(new XmlTransformerUtil()).toBeTruthy();
  });
});
